export interface INaverCredentialsRequest {
  customerId: string;
  apiKey: string;
  secretKey: string;
}

export interface INaverCredentialsResponseData {
  customerId: string;
  apiKey: string;
  secretKey: string;
}
